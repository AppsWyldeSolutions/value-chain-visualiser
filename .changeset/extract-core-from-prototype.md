---
"@AppsWyldeSolutions/vcv-core": minor
---

Extract the value chain KPI logic from the prototype into `packages/core`: `KpiRow`/`Theme` types, `fromWideRecords`/`fromLongRows` ingestion, `evaluate`/`formatValues` for a single row, and `buildModel` to group rows into a `ValueChainVisualiserModel` for a selected year/period.
